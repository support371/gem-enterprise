{
  "framework": "nextjs",
  "buildCommand": "npm run db:generate && npm run build",
  "installCommand": "npm ci",
  "crons": [
    {
      "path": "/api/cron/news-ingest",
      "schedule": "0 */3 * * *"
    }
  ]
}
