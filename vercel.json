{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci --no-audit --no-fund",
  "rewrites": [
    { "source": "/((?!assets|favicon\\.ico|favicon\\.svg|robots\\.txt|placeholder\\.svg).*)", "destination": "/index.html" }
  ]
}
