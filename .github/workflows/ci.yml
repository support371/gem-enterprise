name: CI

on:
  push:
    branches: [main, claude/**]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    name: Lint & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate
        env:
          DATABASE_URL: postgresql://ci:ci@localhost:5432/gem_ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          DATABASE_URL: postgresql://ci:ci@localhost:5432/gem_ci
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-test-secret-min-32-characters-long' }}
          NEXT_PUBLIC_APP_URL: https://gem-enterprise.vercel.app
          NEXT_PUBLIC_APP_NAME: GEM Enterprise
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          NEXT_PUBLIC_AI_DISCLOSURE_TEXT: ${{ secrets.NEXT_PUBLIC_AI_DISCLOSURE_TEXT }}
          AUDIT_ENABLED: 'true'
          # CI-only: no SMTP needed for build
          SMTP_HOST: ''
          SMTP_PORT: '587'
          SMTP_USER: ''
          SMTP_PASS: ''
